# Current control state

## Established results
- Pi runtime implementation, formal registry migration/recovery, role/tool separation, `/sessions` projection, lifecycle/CAS, ControlEvents, managed-path guards, and Goal tools pass `npm run check` and the final 16-suite Pi regression set (97 tests). Coding-agent bundle rebuild completed (56 files, 8.2 MiB).
- Offline recovery probe on the migrated OpenCodeX fixture passed without model requests or listeners: canonical Controller `01a0c135-2af7-764b-90d0-8f692e3a47b5`; foreground was the same Session in this startup (foreground may independently be an Executor); T001 Executor `01a0caf4-b39f-750e-a041-5336c9534cae`; exactly one logical Controller; Controller and Executor tools are separated; `/sessions` lists Controller before selected Executor; T001 remains BLOCKED with unchanged memory, generation 1, one assigned history event; both transcript hashes unchanged.
- Final authority disposition (supersedes stale 3457 references below): `OpenCodeX live migration verification = BLOCKED BY EXTERNAL DEPENDENCY`. User's topology is one OpenCodeX instance/service/PID/listen port at 3456; Pi/Control Plane uses that existing 3456 service, with no separate Pi port, no 3457, no `opencodex-v11.service`, and no second OpenCodeX instance. Do not start or probe 3457. The earlier routing-preset startup failure remains diagnostic history only; do not request a second service or routing change for Pi verification.
- `RUNTIME SEMANTICS CLOSURE = PASS` is recorded in `task-session-native/plan.md` and T001.
- Legacy runtime objects are `control/state/control.sqlite3` (schema 4, WAL mode expected) and `control/assignments.json` (T001 assignment to Session `01a0caf4-b39f-750e-a041-5336c9534cae`; no generation field in legacy record).
- Initial search in the wrong `~/.pi/sessions` path did not find sessions; the actual global namespace is `~/.pi/agent/sessions`. Legacy registry contains four rows, all inactive, and canonical Controller `01a0c135-2af7-764b-90d0-8f692e3a47b5`; assigned Executor `01a0caf4-b39f-750e-a041-5336c9534cae`. Both transcripts and the other two registry Session files exist and their identities match. Executor transcript is 658,699 bytes; Controller transcript is 16,584,344 bytes. Initial SHA-256s recorded in T001. No missing transcript/canonical conflict found.
- Pi implementation removes legacy runtime discovery/fallback and uses formal `.pi/` runtime state with `control/` Task authority.
- OpenCodeX's only authorized live service endpoint is its existing listener on 3456; no second instance or Pi-specific listener is authorized.

## Decisive frontier
Pi implementation and offline migration recovery are verified. Live migration verification is explicitly `BLOCKED BY EXTERNAL DEPENDENCY`, per user decision and topology: one OpenCodeX service/PID/listener at 3456, Pi/Control Plane through that existing service, no 3457 service and no second instance. Resume live verification only when the OpenCodeX owner exposes the migrated PiRoot through the existing 3456 service and authorizes recovery verification within that service. Acceptance: verify canonical Controller, T001 Executor, registry/assignment authority and `/sessions` projection without opening another port or starting another OpenCodeX instance. No Pi-specific network listener is required. Do not modify OpenCodeX routing/provider/GPT-6/source/artifact/candidate/cutover. The bounded GPT-6 continuation trial is not run because existing Task authority conflicts with a real trial.

## Active tasks
- OpenCodeX-owned routing preset reconciliation and authorized bounded GPT-6 continuation scope are external follow-ups; no Pi implementation dependency remains.

## Rollback
Backup exists at `/home/charles/Workspaces/3rdparty/opencodex-piroot-backup-20260923-092500`, including legacy registry/assignments and source transcripts. No 3457 service is active. Do not rollback the verified migrated `.pi/` copy absent evidence of data loss; preserve it for eventual verification through the existing 3456 service.
