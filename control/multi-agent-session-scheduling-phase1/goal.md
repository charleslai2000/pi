# Goal: Pi-native Multi-Agent Session Scheduling — Phase 1

Status: ACTIVE

## Authorized outcome
Implement Pi-native optional Agent profiles and Controller-directed Task execution by fresh ordinary Pi Session tenures, preserving Markdown Task SSOT and existing assignment-generation semantics.

## Scope
- Freeze formal `.pi/` project authority layout and profile/prompt/dispatch/memory architecture.
- Implement profile discovery/validation/override, Task-bounded prompt assembly, Controller-selected agent/cwd execution fork, Session Registry/display projection, and sequential tenure behavior.
- Keep execution transcript out of Task authority; Executors may mutate assigned Task memory only.
- Do not add ExecutionAttempt, subagent runtime, scheduler authority, automatic Task-type routing, or Goal/Plan mutation authority for Executors.

## Completion
Required focused and integrated E2E acceptance passes and `npm run check` passes. Report OpenCode profile migration mapping and observed E2E evidence.
