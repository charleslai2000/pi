# Current control state

# MA4 closure

## Established evidence
- MA3 checkpoint `16083a15b` and MA1/MA2 checkpoint `e0e9098ee` remain the implementation baselines; no production scheduling/runtime changes were made for MA4.
- Deterministic integrated qualification exercises the actual Controller Session tool loop with scripted responses and observes `tool_execution_start` parameters. It proves plumbing and the full implementation → review → remediation → verification workload, plus generic omitted-agent dispatch. Scripted choices are not autonomous model evidence.
- The configured real provider on the existing `127.0.0.1:3456` service produced a real specialist `dispatch_task` call using catalog profile `coder` and a contained project cwd. Fork metadata applied `opencodex/worker` / `medium`; Goal→Plan→Task context was present. In a valid isolated workspace, focused Node tests passed 2/2 including duplicate-key rejection.
- The real generic experiment did not produce `dispatch_task`; that run is not used as acceptance evidence. A particular real model's generic-vs-specialist choice is non-blocking policy variance, not runtime correctness.
- `.pi/AGENTS.md` explicitly states that `agent` and `cwd` are optional; omitting both dispatches ordinary generic Execution at `<PiRoot>`.
- Final gates: integrated E2E 1/1; MA4 focused + MA1–MA3 regressions 12 files / 50 tests; `npm run check` passed.

## Closure
MA4 = PASS. No remaining MA4 work.
